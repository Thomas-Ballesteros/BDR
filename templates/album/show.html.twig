{% extends 'base.html.twig' %}

{% block title %}La Gallerie{% endblock %}

{% block body %}
    

   {# <table class="table">  
   
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ album.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ album.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ album.content }}</td>
            </tr>
        </tbody>
    </table>#}
{% for album in album.images %}

<main>
    <div class="row">
    <div class="col-sm-6">
        <div class="card">
        <div class="card-body">
            
    <img class="img-fluid rounded" src="{{ asset('uploads/' ~ album.name) }}" alt="Image " whidth="300">
    
        </div>
    </div>
    
        </main>
{#<img class="img-fluid rounded" src="{{ asset('uploads/' ~ album.name) }}" alt="Image " whidth="300">#}
{% endfor %}
    <a href="{{ path('album_index') }}">back to list</a>

    <a href="{{ path('album_edit', {'id': album.id}) }}">edit</a>

    {{ include('album/_delete_form.html.twig') }}



{% endblock %}

